import React from 'react';
import {Text, SafeAreaView,Image} from 'react-native';
import Logo from '../'
const Loginscreen = () => {
  return (
    <SafeAreaView>
      <Image src={} />
    </SafeAreaView>
  );
};

export default Loginscreen;
